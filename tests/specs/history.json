[
  {
    "name": "History via history:menu shows empty state when no standups have run",
    "steps": [
      {
        "send": { "callback": "history:menu" },
        "expect": [
          { "method": "editMessageText", "payload": { "text": "No standup history yet. Run a standup from the 📊 Standup menu to get started." } }
        ]
      }
    ]
  },
  {
    "name": "History via history:view shows empty state when no standups have run",
    "steps": [
      {
        "send": { "callback": "history:view" },
        "expect": [
          { "method": "editMessageText", "payload": { "text": "No standup history yet. Run a standup from the 📊 Standup menu to get started." } }
        ]
      }
    ]
  },
  {
    "name": "/history command starts the search flow",
    "steps": [
      {
        "send": { "text": "/history" },
        "expect": [
          { "method": "sendMessage", "payload": { "text": "Search past standups. Send a keyword to filter by team name, or tap Skip to see all." } }
        ]
      },
      {
        "send": { "callback": "history:filter:skip" },
        "expect": [
          { "method": "editMessageText", "payload": { "text": "Filter by member name? Send a name to search for, or tap Skip." } }
        ]
      },
      {
        "send": { "callback": "history:filter:member:skip" },
        "expect": [
          { "method": "editMessageText", "payload": { "text": "Filter by start date? Send a date (YYYY-MM-DD) or tap Skip." } }
        ]
      },
      {
        "send": { "callback": "history:filter:date:skip" },
        "expect": [
          { "method": "editMessageText", "payload": { "text": "Filter by end date? Send a date (YYYY-MM-DD) or tap Skip." } }
        ]
      },
      {
        "send": { "callback": "history:filter:date:end:skip" },
        "expect": [
          { "method": "editMessageText", "payload": { "text": "No standup history yet. Run a standup from the 📊 Standup menu to get started." } }
        ]
      }
    ]
  }
]